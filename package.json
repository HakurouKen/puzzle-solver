{
  "name": "puzzle-solver",
  "private": true,
  "type": "module",
  "scripts": {
    "check:skills": "node scripts/check-skills.mjs",
    "runtime:check": "node scripts/ensure-runtime.mjs",
    "test": "pnpm run check:skills && pnpm -r test",
    "type-check": "pnpm -r type-check",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
