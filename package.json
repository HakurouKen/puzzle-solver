{
  "name": "puzzle-solver",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm -r test",
    "type-check": "pnpm -r type-check",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
